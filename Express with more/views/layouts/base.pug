doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title  #{doctitle}
        block styles
        
    body
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    li.main-header__item
                        a(href="/", class=( (path === '/')? "active" : ""  )) Shop
                    li.main-header__item
                        a(href="/admin/form" , class=( (path === '/admin/form')? "active" : ""  ) ) Add product


        block content 